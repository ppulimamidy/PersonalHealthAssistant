# ===========================================
# DEVELOPMENT CONFIGURATION
# For local development, only Supabase and API URL are required.
# Oura uses sandbox mode by default (mock data, no real API calls).
# ===========================================

# Supabase Configuration (REQUIRED)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
# Use Supabase Dashboard → Project Settings → API Keys → Publishable key
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-supabase-publishable-key
# Backward compatibility only (legacy projects)
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# API Configuration (REQUIRED)
NEXT_PUBLIC_API_URL=http://localhost:8080

# ===========================================
# OURA INTEGRATION (OPTIONAL for development)
# Leave commented out to use sandbox mode with mock data
# Only configure for production with real Oura Ring
# ===========================================
# NEXT_PUBLIC_OURA_CLIENT_ID=your-oura-client-id
# OURA_CLIENT_SECRET=your-oura-client-secret
# NEXT_PUBLIC_OURA_REDIRECT_URI=http://localhost:3000/api/oura/callback

# Feature Flags
NEXT_PUBLIC_ENABLE_AI_INSIGHTS=true
NEXT_PUBLIC_ENABLE_DOCTOR_PREP=true

# Development Mode - uses mock Oura data (set to false for production)
NEXT_PUBLIC_USE_SANDBOX=true
