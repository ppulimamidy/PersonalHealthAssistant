# Lightweight production requirements for Nutrition Service
# Strips heavy ML dependencies (TensorFlow, PyTorch) – uses API-based recognition only

# Core FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings>=2.1.0
python-multipart==0.0.6
python-dotenv>=1.0.0

# Database and ORM
asyncpg==0.29.0
sqlalchemy==2.0.23
alembic==1.12.1

# HTTP client
httpx==0.25.2
aiohttp==3.9.1

# Image processing (lightweight – no TensorFlow/PyTorch)
Pillow==10.0.1

# External API clients (API-based recognition instead of local ML)
openai==1.3.7

# Nutrition databases
numpy==1.24.3

# Caching and Redis
redis==5.0.1

# Authentication and security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Monitoring and logging
prometheus-client==0.19.0
structlog==23.2.0

# Utilities
python-dateutil==2.8.2
pytz==2023.3
